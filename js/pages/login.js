/**
 * Login Page Module
 * Ondeline Tech - App do Técnico
 *
 * Nota: o fluxo de login (submit, toggle senha, Enter) já é tratado
 * integralmente pelo script inline do login.php.
 * Este módulo existe apenas para satisfazer o loadPageModule do app.js.
 */

App.initLoginPage = function() {
    // Nenhuma ação necessária — login.php já gerencia tudo inline.
};
